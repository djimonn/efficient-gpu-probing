# efficient-gpu-probing
Prototype implementation of probing and bound propagation for MILP heuristics, focused on GPU-side changed-bound detection and sparse bound-update compaction to reduce host-device transfer overhead during probing cache construction.
